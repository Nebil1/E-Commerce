@model IEnumerable<ShippingType>
@{
    ViewData["Title"] = "Index";
}

  <div class="container p-3">

    <h3 class="text-primary"> Delivery </h3>

      <div class="row pt-4">
        <div class="col-6">
          <h2 class=text-primary>          </h2>
        </div>

      <div class="col-6">
          <a asp-controller="ShippingType" asp-action="Create" class="btn btn-primary"> 
             <i class="bi bi-plus-circle"></i> &nbsp; Add Delivery option </a>
      </div>

     </div>
  </div>

<table class="table table-bordered table-striped" style="width:68%"> 
    <thead>
        <tr>
            <th> Name </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach(var obj in Model)
        {
            <tr>
                <td width="50%">
                    @obj.Name
                </td>

                <td width="15%">
                    <div class="w-75 btn-group" role="group">
                        <a asp-controller="ShippingType" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary mx-2"> 
                            <i class="bi bi-pencil-square"></i> Edit</a>

                        <a asp-controller="ShippingType" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-danger mx-2"> 
                            <i class="bi bi-trash3"></i> Delete</a>

                    </div>
                    
                </td>
            </tr>
        }
    </tbody> 
</table>
